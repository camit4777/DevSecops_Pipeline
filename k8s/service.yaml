apiVersion: v1
kind: Service
metadata:
  name: petapp
  namespace: petapp
  labels:
    app: petapp
spec:
  type: ClusterIP
  selector:
    app: petapp
  ports:
    - name: http
      port: 80
      targetPort: 8080
